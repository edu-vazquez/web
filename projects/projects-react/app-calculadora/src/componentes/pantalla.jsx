import React from 'react';
import '../css/pantalla.css'

const Pantalla = ({ input }) => (
    <div className='input'>
        { input }
    </div>
);

// Funciones Flecha con Retorno Implícito: Cuando una función flecha retorna un valor directamente sin necesidad de realizar operaciones adicionales, puedes utilizar paréntesis ()

export default Pantalla;